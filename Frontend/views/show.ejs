<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All product</title>
</head>

<body>

    <h1>All Products!</h1>

    <% for(let data of products) { %>
        <% console.log(data) %>
            <div class="container">
                <h1>
                    Name: <%= data.name %>
                </h1>
                <div>
                    Description: <%= data.description %>
                </div>
                <div>
                    Price: <%= data.price %>
                </div>
                <div>
                    In Stock: <%= data.stock %>
                </div>
                <!-- Adding the image tag here -->
                <img src="<%= data.img %>" alt="<%= data.name %> Image">
            </div>
            <% } %>
</body>



</html>